<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="com.utoronto.timemng.app.TaskListActivity" >

    <item android:id="@+id/action_new"
          android:title="@string/add_button"
          android:icon="@drawable/ic_action_new"
          android:showAsAction="ifRoom" />
    <item android:id="@+id/action_prev"
          android:title="@string/prev_button"
          android:icon="@drawable/ic_action_previous_item"
          android:showAsAction="ifRoom"/>
    <item android:id="@+id/action_next"
          android:title="@string/next_button"
          android:icon="@drawable/ic_action_next_item"
          android:showAsAction="ifRoom"/>
</menu>